extends layout

block scripts
  script(src='/javascripts/test.js')

block content
  - if(user)
    h1 Hello, #{user.name.first}.
    h2 Catanode! Home screen 
      a(href="/create") Create Game
  - else
    h2 Please 
      a(href="/login") log in
      |  or 
      a(href="/signup") sign up
      |  to create a game.
